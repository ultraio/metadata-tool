// Placeholder code for metadata tool

const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

const main = async () => {
    console.log('i am metadata tool');
    await sleep(8000);
};

main();
